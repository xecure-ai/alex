# Note: S3 Vectors uses a different namespace (s3vectors) from regular S3
# Until Terraform adds native support, the vector bucket must be created manually

# For now, we'll just define the bucket name as an output
# Students will create the vector bucket via AWS Console as part of the setup